export enum Entity {
  ARTICLES = 'articles',
}

// TODO: Check actions with backend
export enum Action {
  CREATE = 'create',
  READ = 'read',
  READ_OWN = 'readOwn',
  UPDATE = 'update',
  DELETE = 'delete',
}
