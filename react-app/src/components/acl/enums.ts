export enum Entity {
  JOBS = 'jobs',
}

// TODO: Check actions with backend
export enum Action {
  CREATE = 'create',
  READ = 'read',
  READ_OWN = 'readOwn',
  UPDATE = 'update',
  DELETE = 'delete',
}
