import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
  listTitle: {
    color: 'blue',
  },
  subTitle: {
    fontWeight: 'bold',
  },
  content: {
    fontSize: 11,
    fontWeight: 'light',
  },
});

export default styles;
