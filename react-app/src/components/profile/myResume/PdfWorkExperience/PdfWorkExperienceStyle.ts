import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
  listTitle: {
    color: 'blue',
  },
  subTitle: {
    fontFamily: 'Roboto',
    fontWeight: 700,
  },
  content: {
    flexShrink:1,
    flexWrap: 'wrap',
  },
});

export default styles;
