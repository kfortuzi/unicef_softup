import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
  publicationText: {
    fontSize: 12,
  },
  publicationItem: {
    flexDirection: 'row',
    gap: 5,
  },
});

export default styles;
